<UserControl x:Class="Uniation.Views.Pages.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:components="clr-namespace:MainComponents.Components;assembly=MainComponents"
             xmlns:pages="clr-namespace:Uniation.ViewModels.Pages"
             mc:Ignorable="d"
             d:DesignHeight="1080" d:DesignWidth="1920"
             d:DataContext="{d:DesignInstance pages:MainPageViewModel}"
             x:Name="MainPage">
    <Grid>
        <StackPanel>
            <WrapPanel>
                <Button Style="{StaticResource BlueMainMenuButton}" Command="{Binding ToProjectsCommand}" >Наши проекты</Button>
                <Button Style="{StaticResource BlueMainMenuButton}">Об организации</Button>
            </WrapPanel>
            <Button Style="{StaticResource RedMainMenuButton}">Пожертвовать</Button>
        </StackPanel>
    </Grid>
</UserControl>