<UserControl x:Class="Uniation.Views.Pages.DonationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Uniation.Views.Pages"
             xmlns:pages="clr-namespace:Uniation.ViewModels.Pages"
             xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
             mc:Ignorable="d" 
             d:DesignHeight="1080" d:DesignWidth="1920"
             d:DataContext="{d:DesignInstance pages:DonationPageViewModel}">
    <Grid>
        <Button Content="Назад" Margin="-1800,-800,0,0" Height="80" Width="100" Command="{Binding ReturnCommand}"/>
        <Canvas>
            <TextBlock Canvas.Top="10" Text="Выберите проект"/>
            <ComboBox Canvas.Top="30" ItemsSource="{Binding Projects}" SelectedIndex="{Binding SelectedIndex}">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding title}"/>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
            <TextBlock Canvas.Top="60" Text="Укажите сумму пожертвования"/>
            <WrapPanel Canvas.Top="80">
                <RadioButton  GroupName="Sum" IsChecked="{Binding IsOneHundredChecked}">
                    <StackPanel>
                        <b:Interaction.Triggers>
                            <b:EventTrigger EventName="Checked">
                                <b:InvokeCommandAction Command="{Binding UserChooseCommand}"/>
                            </b:EventTrigger>
                        </b:Interaction.Triggers>
                        <TextBlock Text="100P"/>
                    </StackPanel>
                </RadioButton>
                <RadioButton GroupName="Sum" IsChecked="{Binding IsFiveHundredChecked}">
                    <b:Interaction.Triggers>
                        <b:EventTrigger EventName="Checked">
                            <b:InvokeCommandAction Command="{Binding UserChooseCommand}"/>
                        </b:EventTrigger>
                    </b:Interaction.Triggers>
                    <TextBlock Text="500P"/>
                </RadioButton>
                <RadioButton GroupName="Sum"  IsChecked="{Binding IsOneThousendChecked}">
                    <b:Interaction.Triggers>
                        <b:EventTrigger EventName="Checked">
                            <b:InvokeCommandAction Command="{Binding UserChooseCommand}"/>
                        </b:EventTrigger>
                    </b:Interaction.Triggers>
                    <TextBlock Text="1000P"/>
                </RadioButton>
                <RadioButton GroupName="Sum"  IsChecked="{Binding IsFiveThousandsChecked}">
                    <b:Interaction.Triggers>
                        <b:EventTrigger EventName="Checked">
                            <b:InvokeCommandAction Command="{Binding UserChooseCommand}"/>
                        </b:EventTrigger>
                    </b:Interaction.Triggers>
                    <TextBlock Text="5000P"/>
                </RadioButton>
            </WrapPanel>
            <TextBlock Canvas.Top="120" Text="{Binding Sum}"/>
            <TextBlock Canvas.Top="120" Text="Своя сумма" Visibility="{Binding PlaceholderVis}"/>
            <TextBlock Canvas.Top="140" Text="Минимальная сумма пожертвования 10P"/>
            <Button Canvas.Top="160" Content="Отмена" Command="{Binding CancelCommand}"/>
            <Button Canvas.Top="180" Content="Пожертвовать" Command="{Binding DonateCommand}"/>
            <WrapPanel Canvas.Top="200">
                <Button Content="1" Command="{Binding InputSumCommand}" CommandParameter="1"/>
                <Button Content="2" Command="{Binding InputSumCommand}" CommandParameter="2"/>
                <Button Content="3" Command="{Binding InputSumCommand}" CommandParameter="3"/>
                <Button Content="4" Command="{Binding InputSumCommand}" CommandParameter="4"/>
                <Button Content="5" Command="{Binding InputSumCommand}" CommandParameter="5"/>
                <Button Content="6" Command="{Binding InputSumCommand}" CommandParameter="6"/>
                <Button Content="7" Command="{Binding InputSumCommand}" CommandParameter="7"/>
                <Button Content="8" Command="{Binding InputSumCommand}" CommandParameter="8"/>
                <Button Content="9" Command="{Binding InputSumCommand}" CommandParameter="9"/>
                <Button Content="0" Command="{Binding InputSumCommand}" CommandParameter="0"/>
                <Button Content="x" Command="{Binding InputSumCommand}" CommandParameter="-1"/>
            </WrapPanel>
        </Canvas>
    </Grid>
</UserControl>
