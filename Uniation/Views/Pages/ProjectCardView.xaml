<UserControl x:Class="Uniation.Views.Pages.ProjectCartView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Uniation.Views.Pages"
             xmlns:pages="clr-namespace:Uniation.ViewModels.Pages"
             mc:Ignorable="d" 
             d:DesignHeight="1080" d:DesignWidth="1920"
             d:DataContext="{d:DesignInstance pages:ProjectCartViewModel}">
    <Grid>
        <Button Content="Назад" Margin="-1800,-800,0,0" Height="80" Width="100" Command="{Binding ReturnCommand}"/>
        <Button Content="Пожертвовать" Margin="-1800,-700,0,0" Height="80" Width="100" Command="{Binding ToDonationCommand}"/>
        <StackPanel Margin="-1800,0,0,0">
            <TextBlock Width="100" Height="50" Text="{Binding Project.title}" Margin="0,10,0,0"/>
            <Image Source="{Binding Image}" Margin="0,10,0,0"  Width="300" Height="100" />
            <TextBlock Text="{Binding Project.description}" Margin="0,10,0,0"/>
        </StackPanel>
    </Grid>
</UserControl>
