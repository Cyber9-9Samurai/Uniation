<UserControl x:Class="Uniation.Views.Pages.AboutPageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Uniation.Views.Pages"
             xmlns:pages="clr-namespace:Uniation.ViewModels.Pages"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance AboutPageViewModel}">
    <Grid>
        <Button Content="Назад" Margin="-1800,-800,0,0" Height="80" Width="100" Command="{Binding ReturnCommand}"/>
        <StackPanel>
            <Image Source="{Binding ImagePath}" Width="300" Height="300"/> 
            <WrapPanel>
                <Button Content="l" Command="{Binding SwipeCommand}" CommandParameter="-"/>
                <Button Content="r" Command="{Binding SwipeCommand}" CommandParameter="+"/>
            </WrapPanel>
        </StackPanel>
    </Grid>
</UserControl>
